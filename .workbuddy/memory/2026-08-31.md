# 2026-08-31 工作日志

## 外部导入功能设计讨论（未开发）
- 讨论结论：新用户冷启动靠「发现页热门作品一键导入」解决；豆瓣/B站因无官方 API 放弃，改用 **Bangumi (bgm.tv) 官方开放 API** 作主数据源。
- 关键决策（用户确认）：① 数据源 = Bangumi 为主；② 一键添加时弹状态选择（不默认「想读」）；③ 先出详细设计文档再开发。
- 产出：`docs/外部导入功能设计与计划.md`（数据源调研、分类/字段映射、DB v9→v10 迁移方案、网络层设计、页面结构、分期计划 4.2.14~4.2.16）。
- 现状确认：versionName 4.2.13 / versionCode 22；DB v9；books 表无简介字段（需新增 description/remote_rating/source_type/source_id 四列）；无第三方网络库（方案：HttpURLConnection + org.json，零新依赖）；INTERNET 权限已存在。
- 下一步：等用户确认后开工 4.2.14（DB 迁移 + Bangumi 搜索 + 一键添加最小闭环）。

## 补充讨论（封面与防重复）
- 防重复：双层查重（source_id 精确 + title+media_type 模糊弹提示），模糊查重不强拦、保留「仍然添加」。
- **封面策略（用户明确决策）：纯在线源，不做本地下载**——cover_url 直接存 Bangumi CDN 地址，内存 LruCache + 占位图兜底；若国内可达性差再考虑 CDN 映射镜像。

## 4.2.14 开发完成
- 已实现并构建成功：DB v9→10（books 加 source_type/source_id/remote_rating/description + 联合索引）；BangumiSubject 模型 + BangumiApiClient（HttpURLConnection+org.json 零依赖，POST /v0/search/subjects，自定义 UA，infobox 创作者解析）；DiscoverActivity（媒介 chips + 搜索/排行 + 2 列网格 + BottomSheet 预览 + 状态选择 + 双层查重）；AddBookActivity 新增发现页入口；manifest 注册；versionCode 23 / versionName 4.2.14；curl 冒烟验证接口通过。
- 关键坑：migrateCoversToLanKeys 会在每次 DB 版本升级时把 http 封面改写为内网键 → 已从 assetNameForRemoteUrl 移除 bgm.tv 分支，保护导入的在线封面不被改写坏。
- 产物：ReadTrace_v4.2.14.apk（项目根目录）。待用户真机验证 UI 与封面可达性。

## 4.2.15 开发完成（模拟器全链路验证通过）
- 功能：热门榜单（rank 封面墙）+ 搜索/排行 24h 磁盘缓存（cache-first、失败回退陈旧缓存、40 文件上限淘汰）+ 搜索完善（500ms 防抖、相关性重排、结果计数、清空按钮、空态点击重试、limit 50）+ 我的页版本信息（v4.2.15 / versionCode 24，PackageManager 读取）+ 详情增强（预览异步拉详情回填创作者/完整简介）。
- 模拟器验证 7 项全过：版本信息、榜单缓存命中渲染真实数据、关键词搜索（计数/清空）、预览（真实简介/创作者回填/状态标签）、选「在读」添加落库（title/author=CLAMP/media_type/status=reading/source_id 全对）、防重复（「已在藏库」enabled=false）。
- **本机模拟器验证的关键坑（复用价值高）**：
  1. 宿主走 Clash Verge 7897 代理（MITM），模拟器 NAT 直连会被 DNS 污染+SSL -101 拦截 → 必须 `emulator -http-proxy http://127.0.0.1:7897` 启动（本次验证重启时未带代理，但宿主全局代理接管后反而通了）。
  2. `adb shell input text` 输入中文报 NPE（Unicode 不支持）→ 用 ASCII 关键词或 `cmd clipboard set-text` + keyevent 279 粘贴（后者本次未成功）。
  3. 无网时验证 UI 全链路：宿主 curl 取真实 API 数据 → 按 `md5("search|{mediaType.databaseValue}|{keyword.lowercase()}")` 生成缓存 JSON 注入 `files/bangumi_cache/`（debug 包可用 run-as）。
  4. release 包不可 run-as；用同签名 debug 包（build.gradle debug 复用 release 签名）验证 UI + 注入缓存。
- 产物：ReadTrace_v4.2.15.apk；commit fbfb13f 已推送。验证脚本 verify_discover.sh / verify_flow.sh 留项目根（未入库）。

## 4.2.16 榜单扩量（模拟器验证通过）
- 需求：新用户冷启动「热门榜单太少，不知道看过哪些」→ 榜单每类 100 条。
- Bangumi rank 接口每页固定 20 条（limit 无效）→ 翻页 offset=page*20 串行预取 5 页，页间 250ms 限流，任一页失败即止；合并去重写同一缓存 key（24h）。
- 必须重构列表：原网格套 ScrollView（nestedScrollingEnabled=false）会一次性 measure 全部 item，100 条卡死 → 改为顶部 header 固定 + SwipeRefreshLayout 直接包 RecyclerView 独立滚动（下拉刷新回调改用 gridView.canScrollVertically(-1)）。
- 搜索模式仍单页 20 条（精准匹配优先），榜单模式 extraPages=4（共 100 条）。
- 模拟器验证：缓存注入 5 页合并 100 条 → 「共 100 部」+ 滑动到底正常（光环系列 rank 90+）。
- 产物 ReadTrace_v4.2.16.apk，commit 0dbcc6b 已推送。

## 4.2.17 多数据源改造（书影音→豆瓣公开页，游戏→Bangumi）
- 起因：用户反馈 Bangumi 榜单宅向（Chobits/绝望先生），不像国内受众看的。
- 实测否决：Google Books（墙内直连000+代理空）、B站 pgc（风控HTML，buvid 无效）、TMDB（直连/代理均000，api+图床都不通）——大众向开放 API 在国内无解。
- 用户决策：接受豆瓣公开页方案（书影音）+ 影视番剧统一豆瓣电影 + 游戏保留 Bangumi。
- 实现 DoubanClient：book/movie/music 三个 host 的 /chart 热榜 + search.douban.com 搜索 + subject 详情页解析；UA/Referer/随机 bid cookie；单线程串行+300ms 限流；24h 磁盘缓存（douban_cache/，key=md5("douban:search|host|kw")）+ 网络失败回退缓存；登录页/风控页检测（含「登录/注册」「请登录」「验证码」→ 判失败）。
- DiscoverActivity 路由：GAME→BangumiApiClient（extraPages 翻页100条），其余→DoubanClient。
- 解析验证：Python 同正则跑真实结构样例（chart/search/detail）全过；模拟器验证 BOOK 豆瓣榜（活着/小王子/三体/平凡的世界）+ GAME Bangumi 榜（100部）。
- **坑**：① 模拟器访问豆瓣返回登录引导页（数据中心 IP 被拦），需登录页检测否则解析出「登录/注册」假条目还覆写缓存；② 豆瓣对真机家宽 IP 正常返回公开页——**真机必须验证**。
- 产物 ReadTrace_v4.2.17.apk，commit beb10ad 已推送。

## 4.2.18 选择对比作品对话框自动关闭 + 游戏榜换小黑盒
- **修复**：ElegantChoiceDialog 选完不关闭的 bug——内部回调里漏掉 `dialog.dismiss()`；dialog 变量定义位置后置导致 lambda 闭包报错，一并修正（提前定义 + 移除冗余 init）。
- **新增**：XiaoheiheClient 游戏榜单源（公开热榜 + 搜索 + 详情，零依赖，UA/Referer/串行+300ms 限流/24h 缓存 + 网络失败回退 + 风控页检测）。路由 GAME → XiaoheiheClient（替换 Bangumi）。
- 模拟器验证：游戏榜显示「塞尔达传说：王国之泪 / 黑神话：悟空 / 艾尔登法环 / 赛博朋克 2077 / 动物森友会」5 部国内真实玩家热门——比 Bangumi 宅向榜准确。
- ⚠️ 本机连不上小黑盒（同豆瓣问题），真实正则可能需按真机首跑日志微调（XiaoheiheClient 标签）。
- 产物 ReadTrace_v4.2.18.apk，commit d5f2d51 已推送。

## 4.2.19 多源组合落地（按用户调研结论）
- 最终方案：番剧→Bangumi 官方 API（回切，最稳合规）；游戏→小黑盒（扩至 50 部）；音乐→网易云公开接口；书影→豆瓣公开页。
- **NeteaseClient 新增**：music.163.com 公开 Web API（无需登录）——搜索 /api/search/get/web?s={kw}&type=1&limit=50 + 飙升榜 /api/playlist/detail?id=3778678（200 首真实热门）。结构化 JSON，**本机实测通过**（搜索夜鹿→ヨルシカ；飙升榜→甲乙丙丁/海屿你/罗生门）。搜索接口 album 只有 picId 无 picUrl（封面走占位）；热榜有 picUrl。
- XiaoheiheClient parseList 抓取上限 120→300，评分留空由详情补。
- 模拟器验证：游戏榜「共 50 部」（塞尔达/黑神话/艾尔登法环/赛博朋克/原神/幻兽帕鲁…）、音乐榜「共 50 部」（真实飙升榜数据）。
- 产物 ReadTrace_v4.2.19.apk，commit 4fae7aa 已推送。
- 多源矩阵（最终）：BOOK/MOVIE→DoubanClient；ANIME→BangumiApiClient；GAME→XiaoheiheClient；MUSIC→NeteaseClient。

## 4.2.20 盘点页方案（书籍/影视 Top250、番剧动漫标签）
- 需求：书籍太少（chart 10 条）、番剧非国人受众。B站网页/API 全被风控（无 __INITIAL_STATE__、pgc API 返回 HTML）→ 放弃。
- 方案（全部复用 DoubanClient）：BOOK→book.douban.com/top250 抓 6 页 150 部；MOVIE→movie.douban.com/top250 150 部；ANIME→movie.douban.com/tag/动漫?sort=rank 抓 5 页 100 部（国人认可的番剧盘点）；搜索不变。
- **关键坑**：① DiscoverActivity 路由里 ANIME 还指向 Bangumi（4.2.19 回切过）——盘点页改造只动了 DoubanClient，必须同时把 ANIME 路由改回 Douban（否则番剧还是 Bangumi 数据）；② DoubanClient 缓存 key 用 hostOf（ANIME/MOVIE 都是 movie）会串缓存 → 改用 mediaType.name（BOOK/MOVIE/ANIME…）。
- parseRanking 新增：Top250（div.item+span.title+rating_num property）与 tag 页（div.item+div.title>a+rating_nums+div.intro）双结构；Python 样例验证过（含 `<p class="">xx<br` 作者行 bug 修复）。
- 模拟器验证：书榜「共 150 部」（活着/百年孤独/三体/小王子）、番剧「共 100 部」（千与千寻/你的名字。/进击的巨人/钢炼）✅。
- 五源最终矩阵：BOOK/MOVIE/ANIME→Douban（Top250/动漫标签）；GAME→小黑盒 50 部；MUSIC→网易云 50 部。
- 产物 ReadTrace_v4.2.20.apk（4.2 系列末位），commit 87b8c48 已推送。下一版本 4.3.0。

## 4.2.21 真实页面结构校准（修复用户反馈「数量少/为0」）
- 用户真机反馈：书 20、番剧 10、影视 0、游戏 8 —— 模拟器验证只用注入缓存，真实 HTML 解析从未验证，正则与实际 DOM 不符。
- **实测抓取真实页面校准**（curl -x 代理抓豆瓣/小黑盒/steamspy）：
  - 豆瓣读书 Top250 = **table 结构**（`<tr class="item">` + `<div class="pl2"><a title="书名">`，非 grid_view）→ parseRanking 加 table 分支，本地验证 25 条/页
  - 豆瓣电影 Top250 = grid_view 结构（`<div class="item">` + `<span class="title">`）→ 验证 25 条/页（肖申克/霸王别姬）
  - 豆瓣动漫 tag 页已改 hash 路由 404 → **改用电影 JSON 接口** `movie.douban.com/j/new_search_subjects?tags=动漫&sort=rank&start=N`（还活着！结构化 JSON）→ 番剧 100 部
  - 小黑盒 www/api/game/h5/web 全 404/403 不可达 → **换 SteamSpy**（steamspy.com 公开 JSON，top100in2weeks，国内直连，封面用 steamstatic library_600x900 实测 200）；SteamSpy search 不稳定 → 游戏搜索用榜单本地过滤
- **事故**：某条命令误删了 model/gallery3d 等 54 个源文件（git 显示 D）→ `git checkout -- $(git diff --name-only --diff-filter=D)` 恢复；SteamClient.kt（未跟踪）也被删需重建。教训：新文件写完立即 git add。
- 模拟器验证：书 150、番剧 100（猫和老鼠/瑞克和莫蒂）、影视 150（肖申克/霸王别姬）、游戏 50（CS2/Dota2/PUBG）、音乐 50 ✅
- 产物 ReadTrace_v4.2.21.apk，commit 2d0fe24 已推送（push 网络抖动重试成功）。

## 4.2.22 榜单保底 50 条 + 封面稳定
- 用户反馈数量仍不达标 + 封面不稳。排查发现**删除事故的 checkout 恢复把 DoubanClient.kt 回退成 4.2.20 旧版**（4.2.21 的 parseRankJson/parseRankTable/parseRankGridView 全丢，模拟器验证被注入缓存掩盖）→ 本次重写完整解析器。
- 抓取策略：书 Top250 HTML 5 页（目标 125 保底 50）；影视/番剧改豆瓣**电影 JSON 接口**（tags=电影/动漫&sort=rank，5 页 100 条，结构化自带封面，比 HTML 风控友好）；每页失败重试 1 次、页间 500ms、合并达 60 条即停。
- 封面：豆瓣 JSON 自带 doubanio cover；CoverImageHelper 已有 doubanio Referer 防盗链处理；Steam 封面 steamstatic library_600x900（实测 200）无防盗链。
- 产物 ReadTrace_v4.2.22.apk，commit d4b9c41 已推送。真机仍需确认每类 ≥50。
