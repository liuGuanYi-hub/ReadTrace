# 2026-09-01 工作日志

## v1.0.1 微信授权 + 手机号验证码双轨认证（已完成并真机验证）

### 版本号规范变更（用户确认）
- **保留 1.0.0**，后续按 1.0.1 → 1.0.2 → … → 1.0.30 → 1.1.0 顺延；旧 4.2.x 序列废止
- 项目记忆 MEMORY.md 已同步更新

### 新增功能
- `LoginType` 枚举 + CuratorAccount 扩展 4 字段（loginType/wechatOpenId/phoneMasked/thirdPartyAvatarEmoji），JSON 向后兼容
- `WeChatAuthManager`：沙盒/正式双轨；AppID 走 gradle.properties `WECHAT_APP_ID` → BuildConfig（buildConfig=true 已开启）；OpenSDK 用反射调用，不引依赖
- `wxapi.WXEntryActivity`：SDK 强制回调位置；沙盒模式作为模拟授权页（微信绿风格）
- `PhoneAuthManager`：6 位码生成 / 60s 冷却 / 5min 有效期 / 脱敏；`dispatchSms()` 是短信平台接入点
- `OtpInputView`：6 方格 + 1dp 隐藏 EditText 承载键盘（退格/粘贴/输入法全兼容）；错误态用 flashError() 闪红 900ms
- `PhoneAuthActivity`：完整速登页（倒计时用 CountDownTimer，onDestroy 取消；restoreCooldownIfNeeded 防旋转绕过冷却）
- 认证页快捷登录区 + 通行证脱敏绑定徽章；HapticFeedbackEngine 没有 tick()，用 lightClick()

### 模拟器验证结论（uiautomator dump）
- 微信链路：授权页 → 允许 → 通行证「💬 微信 oWX_****xxxx」✓
- 手机号链路：13812348848 → 倒计时 55s → 沙盒明文验证码 → 输入自动校验 → 「📱 138****8848」✓
- 踩坑：
  1. install -r 后旧进程任务栈残留，点击新 Activity 无反应 → 先 `am force-stop` 再测
  2. `exported=false` 的 Activity 无法 adb am start（SecurityException），只能应用内点击
  3. uiautomator dump 偶发干扰 freshly-opened Activity，dumpsys topResumedActivity 确认存活后再 dump
- 注意：force-stop 后模拟器上微信账号昵称会变（沙盒档案随机），测试时账号会被覆盖，属预期

### 遗留
- verify_discover.sh / verify_flow.sh 两个未跟踪脚本（历史遗留，未动）
- release APK 产出 ReadTrace_1.0.1.apk

## 版本口径统一（提交 129896a）
- 用户要求：项目正式版 1.0.1，任何文档/注释不再提及旧 4.2.x 序列
- 清理范围：docs/外部导入功能设计与计划.md（分期表改为 导入一期/导入二期/v1.0.2）、docs/开发进度.md（待办改为「外部导入收尾 v1.0.2」）、3 处 XML 注释（strings.xml / activity_add_book.xml / fragment_profile.xml，仅注释无功能影响）
- 项目记忆 MEMORY.md 版本规范重写，明确「不再提及旧序列」为长期约定
- 删除项目根目录遗留测试 dump：u7~u12.xml、ui6.xml
- 注释级改动未重新构建（XML 注释不影响资源编译，下次发版构建自然覆盖验证）

## v1.0.2 外部导入收尾完成（提交 f98a447，versionCode 38）
- 功能：详情页简介区（3 行折叠 + StaticLayout 溢出判定 + 展开/收起切换）、简介来源标注（按 sourceType 动态显示，手动录入隐藏）、骨架条目「还没有留下你的想法」轻提示（点击跳编辑页）、发现页预览弹窗来源动态化
- 额外修复：renderDescription 中来源为空时 source 视图改为 GONE（防手动录入简介留空行）
- 验证方式：向模拟器库临时插入带长简介的 bangumi 测试条目（id=300），用 dumpsys activity top 读 View bounds 确认折叠 3 行(184px)→展开(696px)→收起；uiautomator 因详情页持续动画拿不到 idle，改用 accent 色(#3A6348)像素扫描定位「展开全文」按钮再 tap；轻提示点击后跳转 AddBookActivity 验证成功；无简介条目(224)与手动条目(1)边界验证通过
- 测试数据已清理，数据库恢复原样；ReadTrace_1.0.2.apk（20MB）签名验证通过（apksigner）
- 经验：详情页有循环动画时 uiautomator dump 会一直报 could not get idle state；dumpsys activity top 的 View 坐标是相对父容器的布局坐标，换算屏幕坐标需叠加 ScrollView 顶部偏移(63) + detailContent 偏移(58) - scrollY

## 详情页转场统一为左右平滑切换（提交 2b92a08）
- 用户发现详情页进入/返回存在两套动效：书架/发现页/媒体中心走共享元素转场（封面放大缩小），其余 15 处入口走系统默认左右滑动
- 处理：移除 LibraryFragment（列表卡+网格卡）、HubFragment（Hero 卡+媒体中心卡）4 处 `makeSceneTransitionAnimation` 调用，改回普通 startActivity；移除 BookDetailActivity 中 `TransitionHelper.setupActivityTransitions` 初始化，全 App 统一系统默认左右滑动
- 验证：模拟器连拍 6 帧确认转场为水平滑动（左侧亮度变化），返回后与进入前书架截图 0% 差异
- 遗留：`util/TransitionHelper.kt` 已无任何调用方（死代码），待用户确认是否删除

## 删除 TransitionHelper 死代码（提交 443ff25）
- 用户确认删除已无调用方的 `util/TransitionHelper.kt`（共享元素转场工具类，69 行）
- 删除后全项目 grep 无残留引用，assembleDebug 编译通过（BUILD SUCCESSFUL）

## v1.0.3 认证正式化完成（提交 6ac5846）
- 阿里云短信正式通道：新建 `util/AliyunSmsClient.kt`（零依赖：HttpURLConnection + 手写 HMAC-SHA1 RPC 签名，Dysmsapi SendSms）；PhoneAuthManager.dispatchSms 移除，requestCode 按 `AliyunSmsClient.isConfigured()` 分流——配置齐全走真实下发（sandboxCode=null），否则沙盒明文，发送失败降级沙盒并带 degraded 标记
- gradle.properties 新增 4 项 ALIYUN_SMS_* 配置 + 注释化 WECHAT_APP_ID；app/build.gradle.kts 增加 4 个 buildConfigField（沿用 findProperty 模式）
- 微信降级完善：currentMode 注释明确双轨启用条件（AppID + app/libs/ OpenSDK aar 缺一不可）；WXEntryActivity 沙盒提示区分「未配置 AppID」与「已配置但缺 SDK」
- 文档：新增 docs/认证正式化接入指南.md（微信 AppID 申请/签名 MD5/OpenSDK 集成、阿里云签名模板申请/安全说明/FAQ）；开发进度.md 更新当前阶段与 v1.0.3 行
- 验证：assembleDebug 通过；模拟器走 CuratorAuthActivity → 手机号速登，未配置阿里云时仍沙盒明文回显验证码（链路无回归）；正式通道待账号到位后实测
- 用户选择保持微信反射方案（不引入 SDK 依赖），短信选阿里云

## v1.0.3 正式打包发布（提交 fd42da2）
- versionCode 38→39，versionName 1.0.2→1.0.3
- 产出 ReadTrace_1.0.3.apk（20.4MB），apksigner 签名验证通过（CN=ReadTrace），aapt2 badging 确认 package/versionCode 39/versionName 1.0.3

## 番剧分类标签过滤（提交 52e49ec）
- 用户诉求：藏库番剧分类下，去掉年份与角色标签，只保留社团/类型
- 实现：LibraryFragment.renderDynamicTags 中按 `selectedMediaType == MediaType.ANIME` 走 shouldFilterAnimeTag
- 过滤规则（保守启发式）：
  - 年份：`^\d{2,4}年?$`（"2018年" / "2022" / "2018"）
  - 角色后缀：含 `酱` / `桑` / `君`（"波奇酱" / "桐人君"）
- 故意去掉"2-4 字纯中文"长度过滤，避免误伤类型词（机甲 / 恋爱 / 日常 等）
- 测试数据验证（4 条测试条目已清理）：年份/角色后缀全被过滤，社团名（京阿尼 / 京阿尼巅峰 / 结束乐队 / 机甲 / 剧场版 / SF / TV）全部保留

## 番剧分类白名单标签（提交 90b09d6）
- 用户进一步明确：只显示 京阿尼 / 麻枝准 / 骨头社 / 催泪神作 / 治愈
- 改 renderDynamicTags：从黑名单过滤改为白名单（`ANIME_TAG_WHITELIST`），番剧分类下非白名单 tag 不聚合
- 删除旧的 shouldFilterAnimeTag / isYearTag / isCharacterTag 启发式（已不需要）
- 验证（截图肉眼确认）：京阿尼(5) / 治愈(4) / 骨头社(3) / 麻枝准(3) / 催泪神作(3) — 全部白名单命中，其他标签（JOJO/Angels of Death/机甲/剧场版/TV 等）均不显示
- 如需增删白名单，直接修改 `ANIME_TAG_WHITELIST` 集合即可

## 外部数据能力扩展 + 文档清理（提交 fbc5835）
- **TMDB 评估**（文档）：`docs/外部导入功能设计与计划.md §8`新增调研结论——暂缓接入（国内可达性差、客户端持 key 风险），推荐服务端代理后重启评估
- **远程评分 → 个人评分联动引导**：详情页新增 `detailRemoteRatingAdopt` 控件，外部导入条目（有 remoteRating 且个人 rating 空）显示「远程评分 X / 5（来源）· 点击采用」，一键写入；新增 `formatNowTimestamp` 工具方法
- **榜单交互优化**：榜单标题带分类名（「🔥 热门榜单 · 番剧」）；空态区分「网络加载失败」与「确实无数据」
- **封面弱网兜底**：CoverImageHelper 网络下载失败时，对预置封面键（LAN key）回退 APK 内置资产，避免裸占位图
- **文档清理**：`docs/开发进度.md` 删除过期风险条目「本轮不运行 Gradle Sync」与 v1.0 时代旧验收标准（标注为历史快照）；补充今日完成的 4 项与 v1.0.4 候选（榜单二次分区、服务端代理后重启 TMDB）
- **模拟器验证**：224 号千与千寻（remoteRating 9.4, rating 空）→ 引导条「✦ 远程评分 4.7 / 5（简介来自 Bangumi）· 点击采用」显示正确 → 点击后 rating 写入 9.4 → 测试数据清理

## 修复：游戏榜单占位条目（提交 2e6717b）
- 用户反馈：【发现】→【游戏】只有 20 个，其余全是占位
- 根因：steam_cache 缓存被污染——50 条中 20 条真实（id 730-749 连续）+ 30 条「Steam Game N」占位（id 2020+ 连续，非 steamspy 真实返回格式）；且 steamspy 现被 Cloudflare 反爬返回 403
- 修复：
  1. SteamClient.parseRank / readCache 双入口过滤「Steam Game」前缀占位名（防御脏数据）
  2. 清理模拟器 steam_cache 脏缓存
  3. 主源失败时 RankRepository 既有逻辑自动降级 Bangumi 补位（验证生效）
- 验证：模拟器发现页游戏分类 → 显示真实 Bangumi 条目（动物之森/无尽的未知/海格力斯的荣光等），来源「Bangumi 补位」，滚动加载 20+ 条零占位
- 注意：steamspy 403 为外部环境变化，真实设备上游戏榜将主要依赖 Bangumi 补位；若需 Steam 数据需另寻数据源或代理

## 游戏榜单换源杉果商城（提交 84d5714）
- 用户诉求：【发现】→【游戏】不要小众 Bangumi 补位条目，要认识的 Steam 单机大作，建议从卖 CDK 的游戏平台爬取
- 数据源调研实测：Steam 官方商店本机 000 不可达；小黑盒 API 404；杉果（sonkwo.hk）200 可达、3DM 商城（shop.3dmgame.com）200 可达
- 选定杉果 hk 首页：29 款知名单机（黑神话/FF16/人中之龙系列/生化危机/文明VI/P5R/Palworld 等），国内可达、带中文标题/简介（game_sku JSON）+ 封面（s8.sonkwo.com）
- 实现：SteamClient 换源——fetchRankListSync 抓 https://www.sonkwo.hk/ HTML，parseRankHtml 双结构解析（内嵌 game_sku JSON + 轮播卡片封面），getSubjectDetail 直接返回原条目（简介已含）；RankRepository 来源名改「杉果热门」；strings 来源标注改「简介来自杉果商城」
- 验证：本机抓真实页面跑解析逻辑 → 29 款全部知名单机 ✓；编译通过 ✓；模拟器无网时降级 Bangumi 补位（符合设计）
- 注意：杉果首页为运营位轮播，游戏列表会随活动变化，但始终是热门单机；缓存 24h

## v1.0.4 正式打包发布（提交 317bed7）
- versionCode 39→40，versionName 1.0.3→1.0.4
- 产出 ReadTrace_1.0.4.apk（20.4MB），apksigner 签名验证通过（同一 release 密钥），aapt2 确认 package/versionCode 40/versionName 1.0.4
- 本版本内容：认证正式化、远程评分联动、榜单交互优化、封面弱网兜底、游戏榜单换源杉果、番剧标签白名单、详情页转场统一
