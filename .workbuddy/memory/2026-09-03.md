# 2026-09-03

## 首页恢复旧版并移除 5 张媒介大卡

**触发**：用户反馈当前首页「太空」，要求恢复之前的丰富版本，但去掉首页 5 个模块（书/动画/电影/游戏/音乐 5 张媒介入口大卡）。

**根因溯源**：通过 git log 定位到 9 月 2 日凌晨 `ea94b8c 优化：主页晨报流极致减法、单行44dp晶体工坊微胶囊…` 把首页从 1290 行布局 / 894 行 Fragment 砍到 400+362 行，把 5 张媒介大卡连同大量结构一并删除，用户体验上感觉空。

**操作**：
- 从 `1c7c8e6`（极致减法前一个提交）`git checkout` 恢复 `fragment_hub.xml` + `HubFragment.kt`。
- 用 Python 在布局中精确删除 5 张媒介大卡（610-1144 行）共 535 行。
- 重新插入之后 3 个 commit 新增的内容到恢复后的布局：
  - 胶囊行（含精神探索、StandBy、黑胶、藏书票、编年、3D 画廊、心智拓扑 7 个胶囊）
  - 那年今日 · 时光回溯便签
- 在 HubFragment.kt 中：
  - 删除 5 张卡的 lateinit/绑定/监听/renderHubCard 调用与函数/速记弹窗函数
  - 补回胶囊交互、那年今日逻辑、滚动进场列表
  - 移除 `databaseHelper.close()`（92e6f2e 架构修复：全局单例防误关，不能在 Fragment onDestroyView 关）
  - 移除 `TransitionHelper` 共享元素转场（2b92a08 移除了该辅助类，回退到普通 startActivity）
  - `+` 按钮保留 QuickLogBottomSheet（P11 极简心流，替代旧的 showQuickRecordDialog）
  - 清理未使用的导入（LinearLayout、ProgressBar、CardView、AnimeTimelineScrollActivity、MediaHubActivity、dpToPx、roundToInt）

**验证**：
- `./gradlew :app:assembleRelease --max-workers=1` BUILD SUCCESSFUL（首轮因 TransitionHelper 报 Unresolved reference，修掉后通过）。
- 用 Python 正则对比 `R.id.*` 引用与布局 `@+id/`，49 个引用全部命中，5 个容器级 id 未被引用属正常。
- 模拟器安装后截图确认渲染正确，5 张大卡确实消失；新增的胶囊行、羊皮纸便签、5 维统计、时光漫忆大卡全部出现。

**遗留问题**：
- 胶囊行除「✨ 精神探索」外，其余 6 个胶囊在浅色模式下视觉上空空如也（只有圆角描边没有图标文字）。根因是它们共用 `bg_glass_panel` 背景，而该 drawable 自 v3.7 起就在内部加了 20dp 内边距，导致 42dp 高的胶囊内容区只剩 2dp 装不下图标+文字。属**历史遗留 bug**，本次恢复一并带来；最小改动原则下未自作主张去改 42 个引用处的公用 drawable，需要时可单独立项修复（推荐为胶囊单独建 `bg_chip_capsule.xml` 不带内边距）。

**变更文件**：
- `app/src/main/res/layout/fragment_hub.xml`（1290 → 991 行）
- `app/src/main/java/com/example/readtrace/ui/fragment/HubFragment.kt`（894 → 597 行）
- `app/build/outputs/apk/release/app-release.apk`（重打成功）

**第二步**：修复首页胶囊行 6/7 胶囊的视觉 bug（空圆角描边）——新建 `bg_chip_capsule.xml`（去 bg_glass_panel 的 20dp 内边距，用 readtrace_chip+glass_stroke 主题色），仅替换胶囊行 6 处 `android:background`，不动其他 42 处 `bg_glass_panel` 用法。模拟器截图验证：浅色态 7 个胶囊全部正常显示图标+文字。

**第三步**：commit fe21a57「还原：首页恢复旧版丰富布局并移除 5 张媒介大卡并修复胶囊视觉」并 push（绕开 127.0.0.1:7897 不可达 proxy：`git -c http.proxy= -c https.proxy= push origin main`）。

**第四步**：按项目发版约定，产物拷贝到项目根 `ReadTrace_1.0.5.apk`（45MB，比 1.0.4 的 20MB 翻倍，怀疑是这期间引入了 barhopper / image_processing_util 等 ML Kit 原生 so 且未能 strip，需留意；*.apk 已被 .gitignore 忽略不入库）。模拟器已 `adb emu kill` 关闭。

**本机新踩坑**：git 全局/本地都配了 `http.proxy=http://127.0.0.1:7897`，代理软件没开时 push 会 `Failed to connect to github.com:443 over proxy 127.0.0.1`。绕过办法是在命令行临时清空：`git -c http.proxy= -c https.proxy= push origin main`（可与 GCM 覆盖参数一起用，互不冲突）。

**待确认的后续优化**：Hero 大位默认会显示整篇 AI 生成的「核心主旨/人物/分幕大纲」（因 `shortComment → review → notes` 优先级取到了长文本），把首屏 2/3 都占满，与「策展金句」的设计意图不符。可选方案：给 `heroBookQuote` 加最大行数折叠（如 3 行 + 「展开全文」），或取长文本的首句/首段。属行为变更，等用户确认后再做。

---

## P35 计划：主页分页化重塑（首屏独尊 Hero + 二页以后沉浸探索）

**触发**（紧接上一段 Hero 折叠探索）：用户提出新诉求——直接在截图里给出版本：「把这个很长的随想去掉，然后把 [Hero 截图] 这个地方扩充成一页居中布置，然后下面那些布局放到第二页往后放下去。也就是说主页一看到就是一个很清爽的记录，然后往下滑才会看到其他东西。」明确指令是**写入开发计划**，不是立即实施。

**写入位置**：`readtrace_project_plan.md` 第 45 章（追加 188 行，文档从 2374 → 2562 行）。注意：最初误编号为「## 35.」与已有第 35 章（P19 微信小程序）重复，已重编号为「## 45. P35」，内部小节 45.1~45.7；章节号是 45，功能代号仍叫 P35（延续 P13/P19 的代号序列）。

**核心架构升维**：
- P13（29 章）当前是「首屏极简（4 大聚焦：Hero + 那年今日 + 晶体工坊 + 统计胶囊）」
- P35 升级为「首屏独尊（1 个核心：Hero 居中）」，其他全部塞进第二页以后滚动可见
- 实质：P13 砍掉 5 张大卡但首屏仍偏杂，P35 进一步把首屏收窄到 1 张 Hero 卡

**关键决策**：
- **废弃**：上一段提到的 Hero 折叠探索（maxLines=3 + 展开全文 toggle）。P35 直接移除 `heroBookQuote` + `heroQuoteToggle` 两个控件，不折叠、不展开，整段不要。
- **保留**：P13 已实施的成果（5 大媒介卡移除 / 7 个晶体工坊胶囊 / 那年今日 / 24h 四时光晕）。
- **数据源**：`Book.shortComment` / `Book.review` / `notes` 字段保留，详情页照常使用；仅 Hero 大位不再消费 quote。
- **版本号**：P35 实施后 `versionCode=42` `versionName=1.0.6`，产出 `ReadTrace_1.0.6.apk`。

**当前工作区状态（冲突）**：
- 已修改未提交：`fragment_hub.xml`（加了 heroQuoteToggle、heroBookQuote 的 maxLines=3）、`HubFragment.kt`（加了 heroQuoteToggle 字段、折叠/展开 listener、临时验证 Log.d("HeroFold", ...)）
- 这些与 P35「整段移除 heroBookQuote」**直接冲突**
- P35 Phase 1 实施时这些会一并清理，所以不必单独回滚；也可以现在就提前清理，节省 Phase 1 工作量

**验证 Hero 折叠展开功能时踩到的坑**：
- `adb shell input tap` 第一次点 (580,717) 完全没反应，根因是猜测坐标错位
- 改用 `dumpsys activity top` 抓取 View 层级 + Python 累加祖先链 mLeft/mTop，得到 heroQuoteToggle 屏幕绝对坐标 (794,1387)-(980,1446)，中心 (887,1416)
- 点击 (887,1416) 验证展开生效：`maxLines=2147483647`、`toggleText=收起 ▴`，确认展开功能正常
- 第二次点击 (887,1416) 无日志，根因是展开后文本从 265px 撑到 1768px，按钮被推到屏幕外 (y=2931，超出 2400 屏高 549px)，同一坐标变成 Hero 正文区域，触发 Hero 卡片点击跳转到 BookDetailActivity
- 重新算坐标后发现 `dumpsys activity top` 输出的是**布局坐标，不含 ScrollView scrollY**，是 View.mLeft/mTop 相对父的固定值。这一发现对所有涉及「屏幕坐标点击」类的 adb 验证场景都通用

**P35 实施路线**：
- Phase 1：fragment_hub.xml 移除 heroBookQuote/heroQuoteToggle，HubFragment.kt 清理相关字段与折叠逻辑，Hero 改 ConstraintLayout 居中
- Phase 2：多分辨率 Hero 封面尺寸自适应（4.7 / 5.5 / 6.7 寸）
- Phase 3：Hero 极光与背景融合度打磨

**用户当前态度**：明确只要文档，未要求立即代码；按全局规则第 9 条给下一步建议并询问是否执行。
---
## P35 Phase 1 全部落地并发布 1.0.6（当日续）
- **完成内容**：① 首屏独尊正方形记录台（firstScreenStage minimumHeight = hubScroll.height - paddingTop，headerPanel minimumHeight = width + layout_gravity center）；② 记录台杂志式充实（眉标 PERSONAL ARCHIVE/VOL.I、五媒介统计网格、2 行按钮网格解决回收站按钮截断、页脚铭文、进场动画）；③ 首页滚动条 scrollbars="none"；④ 我的最爱 favShowcaseSection 加入主页第二页（那年今日后），无收藏 GONE，卡片封面 + 金色 NO.x 角标，无封面走 loadCover(cover,null,placeholder) 占位降级；⑤ 主页导入书单弹窗弃 AlertDialog 改 bg_elegant_dialog 玻璃容器风格（行式选项渐入 + springTouch + 取消胶囊）；⑥ 回收站 ScrollView 加 scrollbars="none"
- **验证**：dumpsys 视图树确认 favShowcaseSection y=3644 在可视区 2337 外（首屏不露）；收藏卡全部 cover V/placeholder G（coverUrl 非空）；模拟器无网导致远程封面空米色底属环境非缺陷
- **模型限制备忘**：本会话后期 Read 无法读图（非多模态）→ 改 dumpsys 程序化验证；release 包 run-as 不可用（package not debuggable），adb root 也被拒
- **Git**：3 个积压 commit（923d8ec/a89cc79/5656a0c）+ 本批 UI 收尾改动待提交推送；远程 origin/main=fe21a57 无冲突
- **弹窗风格待确认**：用户截图指「导入书单二级弹窗」需 app 风格，已重写主页导入弹窗；因读图受限未能 100% 对齐截图所指入口，若用户指系统 CSV 文件选择器（SAF）则无法自绘
