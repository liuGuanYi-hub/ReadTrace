# 阅痕 ReadTrace 开发文档

## 1. 当前主线

阅痕 ReadTrace 当前主线是 Android 本地 App。

当前 Android 工程位于仓库根目录，不再使用旧的 `android/` 子目录结构。

安卓主线详细说明见：

```txt
docs/安卓开发文档.md
```

---

## 2. 当前工程结构

```txt
settings.gradle.kts
build.gradle.kts
gradle.properties
gradle/
app/
docs/
archive/
```

主模块：

```txt
:app
```

归档目录：

```txt
archive/web-react-vite/
archive/fastapi-backend/
```

早期 Web/FastAPI 代码只作为原型参考，不作为 v1.0 主线。

---

## 3. 当前工程事实

当前 Android 配置：

- project name：`readtrace`
- namespace：`com.example.readtrace`
- applicationId：`com.example.readtrace`
- compileSdk：36
- minSdk：31
- targetSdk：36
- Android Gradle Plugin：9.1.1
- 当前 UI：XML layout + AppCompat

当前入口：

```txt
app/src/main/java/com/example/readtrace/MainActivity.kt
```

---

## 4. 产品目标

v1.0 只做安卓本地书籍记录核心功能：

- 添加书籍
- 查看书籍列表
- 查看书籍详情
- 编辑书籍
- 软删除 / 归档书籍
- 阅读状态
- 评分
- 标签
- 简短评价
- 读后感
- 本地保存数据

v1.0 不做登录、云同步、AI、摘录和笔记。

---

## 5. 数据原则

- 不允许物理删除用户数据。
- 删除默认使用软删除 / 归档。
- `tags` 使用 JSON 字符串保存。
- 评分可为空，填写时范围为 1-10，允许一位小数。
- `created_at` / `updated_at` / `deleted_at` 使用 ISO 8601。
- `start_date` / `finish_date` 使用 `YYYY-MM-DD`。

---

## 6. 下一步

推荐下一步：

1. 先稳定当前 Android 工程同步问题。
2. 再设计本地 SQLite 表。
3. 再实现书籍 CRUD 和页面。

任何可能减少 C 盘容量的操作，都必须先说明预计增加量，并等待确认。
