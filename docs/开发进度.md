# 阅痕 ReadTrace 开发进度

## 当前阶段

当前阶段：v1.0 首个可用闭环完成，等待运行验证。

本阶段目标：

- 添加书籍
- 使用原生 SQLite 保存
- 返回首页立即显示书籍
- 支持基础阅读状态筛选
- 不运行 Gradle Sync 或构建

---

## 版本进度

| 版本 | 目标 | 状态 |
|---|---|---|
| v1.0 | Android 本地书籍记录核心功能 | 开发中 |
| v1.1 | 摘录和笔记 | 未开始 |
| v1.2 | 统计和搜索优化 | 未开始 |
| v2.0 | 作品类型扩展 | 未开始 |
| v3.0 | 登录同步和云端版本 | 未开始 |

---

## 当前工程事实

- Android 工程在仓库根目录
- 主模块是 `:app`
- project name 是 `readtrace`
- namespace 是 `com.example.readtrace`
- applicationId 是 `com.example.readtrace`
- 当前 UI 是 XML layout + AppCompat
- 当前首页是 iOS + Mac 玻璃冥想风动态书架页
- 当前入口是 `app/src/main/java/com/example/readtrace/MainActivity.kt`
- 当前 Gradle 配置使用 Android Gradle Plugin `9.1.1`
- 当前数据库是 `readtrace.db`，版本为 `1`

---

## 已完成

- 已确认项目主线为 Android 本地 App
- 已确认早期 Web/FastAPI 代码放在 `archive/`
- 已新增 `docs/安卓开发文档.md`
- 已更新 `docs/开发文档.md`
- 已更新 `docs/开发进度.md`
- 已记录当前工程结构为根目录 Android 工程
- 已记录 v1.0 不做登录、云同步、AI、摘录和笔记
- 已记录删除必须使用软删除
- 已将 Hello World 替换为阅痕静态书架首页
- 已新增玻璃风圆角背景、按钮和状态筛选资源
- 已新增轻微淡入和上移动画
- 已更新首页文案和配色
- 已新增 `Book` 和 `BookStatus` 数据模型
- 已使用 `SQLiteOpenHelper` 建立 `books` 表
- 已实现添加书籍玻璃表单
- 已实现书名、评分和阅读日期校验
- 已实现标签去空、去重和 JSON 字符串保存
- 已实现首页 `onResume` 自动刷新
- 已实现动态书籍卡片
- 已实现全部、想读、在读、已读筛选
- 已确保查询排除软删除记录
- 数据层没有物理删除方法

---

## 进行中

- 等待 Android Studio 运行验证添加与本地持久化闭环

---

## 待办

### v1.0 待办

- 手动验证首次添加、重启持久化和状态筛选
- 实现书籍详情页
- 实现编辑书籍
- 实现软删除 / 归档逻辑
- 实现标签筛选
- 实现搜索
- 完成真机或模拟器运行验证

### v1.1 待办

- 设计 Note 数据模型
- 设计 notes 本地表结构
- 实现笔记列表
- 实现添加笔记
- 实现编辑笔记
- 实现归档笔记
- 在书籍详情页展示摘录和笔记

---

## 风险记录

- 不允许物理删除用户数据，所有删除默认必须走软删除。
- 任何可能减少 C 盘容量的操作，都必须先说明预计增加量并等待确认。
- v1.0 不做笔记和摘录，避免第一版范围变大。
- v1.0 不做登录、云同步和 AI。
- 本轮不运行 Gradle Sync、不构建、不下载依赖。

---

## 本轮验收标准

- 首次打开显示空书架
- 缺少书名时不能保存
- 非法评分和日期显示错误
- 保存后首页立即显示书籍卡片
- 重启 App 后书籍仍存在
- 全部、想读、在读、已读筛选结果正确
- 数据层不存在物理删除 SQL
- 文档明确不物理删除用户数据
- 本轮未运行 Gradle Sync、未构建、未下载依赖
