<?xml version="1.0" encoding="utf-8"?>
<!-- 发现页条目卡片：封面 + 中文名 + 远程评分角标（2 列网格） -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    android:background="@drawable/bg_glass_panel"
    android:elevation="6dp"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="200dp">

        <ImageView
            android:id="@+id/itemDiscoverCover"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/discover_cover_desc" />

        <FrameLayout
            android:id="@+id/itemDiscoverPlaceholder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_book_placeholder"
            android:visibility="gone">

            <TextView
                android:id="@+id/itemDiscoverPlaceholderEmoji"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:textSize="34sp" />
        </FrameLayout>

        <!-- 远程评分角标 -->
        <TextView
            android:id="@+id/itemDiscoverRating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|top"
            android:layout_margin="8dp"
            android:background="@drawable/bg_status_chip_selected"
            android:paddingStart="8dp"
            android:paddingTop="3dp"
            android:paddingEnd="8dp"
            android:paddingBottom="3dp"
            android:textColor="@color/white"
            android:textSize="11sp"
            android:textStyle="bold" />

        <!-- 已收藏角标（双层查重命中时显示） -->
        <TextView
            android:id="@+id/itemDiscoverOwnedBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start|top"
            android:layout_margin="8dp"
            android:background="@drawable/bg_status_chip_selected"
            android:paddingStart="8dp"
            android:paddingTop="3dp"
            android:paddingEnd="8dp"
            android:paddingBottom="3dp"
            android:text="@string/discover_owned_badge"
            android:textColor="@color/white"
            android:textSize="11sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <!-- 快捷添加按钮（免弹窗直接纪念为已看，已收藏时隐藏） -->
        <TextView
            android:id="@+id/itemDiscoverQuickAdd"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_gravity="end|bottom"
            android:layout_margin="8dp"
            android:background="@drawable/bg_status_chip_selected"
            android:clickable="true"
            android:contentDescription="@string/discover_quick_add_desc"
            android:focusable="true"
            android:gravity="center"
            android:text="＋"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <!-- 批量模式选中标记（卡片中央对勾） -->
        <TextView
            android:id="@+id/itemDiscoverSelectedRing"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_status_chip_selected"
            android:gravity="center"
            android:text="@string/discover_selected_mark"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:visibility="gone" />
    </FrameLayout>

    <TextView
        android:id="@+id/itemDiscoverTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:lines="2"
        android:paddingStart="10dp"
        android:paddingTop="8dp"
        android:paddingEnd="10dp"
        android:textColor="@color/readtrace_ink"
        android:textSize="13sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/itemDiscoverDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:paddingStart="10dp"
        android:paddingTop="2dp"
        android:paddingEnd="10dp"
        android:paddingBottom="10dp"
        android:textColor="@color/readtrace_muted"
        android:textSize="11sp" />
</LinearLayout>
