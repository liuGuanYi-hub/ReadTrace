<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/readerRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_readtrace_home"
    android:orientation="vertical">

    <!-- 顶部控制栏 -->
    <LinearLayout
        android:id="@+id/readerTopBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="12dp"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/readerBookTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="sans"
                android:maxLines="1"
                android:textColor="@color/readtrace_ink"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="小王子" />

            <TextView
                android:id="@+id/readerAuthorName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:fontFamily="sans"
                android:maxLines="1"
                android:textColor="@color/readtrace_muted"
                android:textSize="12sp"
                tools:text="圣埃克苏佩里" />
        </LinearLayout>

        <TextView
            android:id="@+id/readerImportTxtButton"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:background="@drawable/bg_secondary_button"
            android:clickable="true"
            android:focusable="true"
            android:fontFamily="sans"
            android:gravity="center"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="📂 换TXT"
            android:textColor="@color/readtrace_ink"
            android:textSize="12sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- 主题快捷选择条 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingBottom="8dp">

        <TextView
            android:id="@+id/themeParchment"
            style="@style/ReadTraceStatusChip"
            android:background="@drawable/bg_status_chip_selected"
            android:clickable="true"
            android:focusable="true"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/reader_theme_parchment"
            android:textColor="@color/white"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/themeMint"
            style="@style/ReadTraceStatusChip"
            android:layout_marginStart="8dp"
            android:clickable="true"
            android:focusable="true"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/reader_theme_mint"
            android:textColor="@color/readtrace_ink"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/themeNight"
            style="@style/ReadTraceStatusChip"
            android:layout_marginStart="8dp"
            android:clickable="true"
            android:focusable="true"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/reader_theme_night"
            android:textColor="@color/readtrace_ink"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- 3D 拟真翻页 ViewPager2 -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/readerViewPager"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>

    <!-- 底部控制栏 -->
    <LinearLayout
        android:id="@+id/readerBottomBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <SeekBar
                android:id="@+id/readerSeekBar"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:max="100" />

            <TextView
                android:id="@+id/readerPageIndicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:fontFamily="sans"
                android:textColor="@color/readtrace_ink"
                android:textSize="13sp"
                android:textStyle="bold"
                tools:text="1 / 48 页 (2%)" />
        </LinearLayout>

        <!-- 一键生成摘录按钮 -->
        <TextView
            android:id="@+id/readerAddExcerptButton"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_primary_button"
            android:clickable="true"
            android:elevation="4dp"
            android:focusable="true"
            android:fontFamily="sans"
            android:gravity="center"
            android:text="@string/reader_btn_add_excerpt"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>

</LinearLayout>
