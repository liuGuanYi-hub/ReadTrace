<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1B263B"
    android:orientation="vertical">

    <!-- 顶部操作栏 -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="36dp"
        android:paddingBottom="12dp">

        <TextView
            android:id="@+id/btnPassportBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_pill_button"
            android:paddingHorizontal="14dp"
            android:paddingVertical="7dp"
            android:text="← 返回"
            android:textColor="#E8E2D9"
            android:textSize="13.5sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="🛂 精神巡礼护照"
            android:textColor="#E0A96D"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/btnPassportExportTop"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_status_chip_selected"
            android:paddingHorizontal="14dp"
            android:paddingVertical="7dp"
            android:text="📤 导出长图"
            android:textColor="#FFFFFF"
            android:textSize="13.5sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <!-- 签证页分类切换器 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/tabPassportAnime"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="6dp"
            android:layout_weight="1"
            android:background="@drawable/bg_status_chip_selected"
            android:gravity="center"
            android:paddingVertical="8dp"
            android:text="🌸 追番入境签证 (71部)"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tabPassportGame"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_weight="1"
            android:background="@drawable/bg_status_chip"
            android:gravity="center"
            android:paddingVertical="8dp"
            android:text="🎮 游戏通关签证 (50+款)"
            android:textColor="#C5BCAD"
            android:textSize="13sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- 护照可滚动印章内页 -->
    <ScrollView
        android:id="@+id/passportScrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:overScrollMode="never"
        android:scrollbars="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:orientation="vertical"
            android:paddingHorizontal="12dp"
            android:paddingBottom="40dp">

            <com.example.readtrace.widget.CulturalPassportView
                android:id="@+id/culturalPassportView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:elevation="10dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="18dp"
                android:text="💡 点击任意印章可查看作品全息卡片与六维心智"
                android:textColor="#A0C4E2"
                android:textSize="12.5sp" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
